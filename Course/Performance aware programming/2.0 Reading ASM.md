This part we were mostly dealing and learning in steps to decode/ execute the 8086
instruction set to implement the basic instructions.

This part majorly contributed to getting familiar with assembly language.

There is not a lot of notes to make here about the how the assembly language is structured.

The basic understanding is for how the 8086 implements various basic instructions like the mov / add / sub / cmp in the given smaller 16 bit register sets and 1MB of memory.

This understanding can be extrapolated to the understanding of the x64 systems which is based on 8086 as backward compatible where the increase in the instruction size and the notation is done by adding identifiers to the front of the existing reigster name with other minor improvements

